<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.upit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MembershipJoin"></activity>
        <activity android:name=".Membershipnew" />
        <activity android:name=".Shopping_cart" />
        <activity android:name=".Purchase_history" />
        <activity android:name=".Like" />
        <activity android:name=".My_Point" />
        <activity android:name=".Membership" /> <!-- activity android:name=".My_Page" / -->
        <activity android:name=".My_Page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>